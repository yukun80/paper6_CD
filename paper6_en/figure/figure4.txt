图4：ChatGPT网页端image2单尺度CQI重绘提示词

一、使用说明
对应方法3.2节HCE内部的CQI查询交互。保留紧凑双层布局，本版统一尺度符号、两次注意力端口和Reshape后的密集残差。将第二部分完整复制即可重新生成图片；如编辑既有图，使用第四部分的唯一局部修订提示词。第二、四部分独立可用。
当前JPG仍为待修正工作图，本轮仅更新本TXT，不生成或替换图片，不修改正文及算法，不运行LaTeX构建。旧“只改输入、其他连接保持”指令失效，不再作为绘图约束。

二、完整英文重绘提示词（整体复制）
Create one compact scientific deep-learning module diagram showing single-scale Change Query Interaction within HCE. Use a white landscape canvas approximately 1.6:1. Preserve a coherent two-tier organization: paired input construction on the left, query update above, spatial-token update below, and dense output on the right. Do not reproduce wiring from an earlier generated image; the connection specification below is authoritative.

STYLE AND EXACT LETTERING
Use Times New Roman-like lettering, italic mathematical variables, thin black orthogonal arrows, restrained pastel fills, thin feature slabs, and segmented query/token strips. Pre-event inputs are pale blue, post-event inputs pale orange, queries pale green, and fused spatial features neutral gray. Small operator rectangles and circled addition symbols carry the calculation. Use subtle local backgrounds, no three-panel split, no large text boxes, no decorative shadows or attention heatmaps.

The following LaTeX expressions specify mathematical typesetting. Render their mathematical appearance, not literal backslashes, braces, underscores, or dollar signs. Use ordinary uppercase A and B as superscripts, a lowercase italic i as the scale subscript, and true numeral 0/1 superscripts for query states. Never replace A/B with Unicode small capitals or omit i.
Input slabs: X_i^A and X_i^B.
Directed difference: X_i^B-X_i^A.
Absolute difference: |X_i^B-X_i^A|.
Dense paired feature: Z_i.
Original tokens: T_i.
Learnable and updated queries: Q_i^0 and Q_i^1.
Updated spatial tokens: T_i^1.
Output: D_i.
Query normalization: \mathrm{LN}_{q,i}.
Token normalization, used twice: \mathrm{LN}_{t,i}.
Attention operation labels: \mathrm{MHA}_1 and \mathrm{MHA}_2.
Attention port labels are plain “Q” and “K/V”, distinct from the mathematical query-state symbols.
Other exact labels: “C”, “Pair proj”, “Flatten”, “FFN”, “Reshape”, “Conv”, “CBAM”, “Tokens → queries”, “Queries → tokens”, and “Shared token LN”. Do not add explanatory sentences inside operator boxes.

LAYOUT TO PREVENT MISCONNECTIONS
Keep the upper query update compact and reduce the unused top margin. Position Q_i^1 above the second attention so it connects downward to that attention's K/V port. Original T_i branches upward to the first attention and toward the second attention without duplication.

Place Reshape immediately next to the FINAL addition node. Arrange Conv, CBAM, and D_i on a short continuation at the lower right if required. Do not place an isolated feature slab beyond Reshape. Reserve a clear outer route for the Z_i residual. Keep the other three residuals as short local loops. No arrow may branch between the second token normalization and Reshape. Their connecting segment is a single unbranched edge.

FOUR INPUTS AND CONVOLUTIONAL PROJECTION
X_i^A and X_i^B each connect directly to C once. Both also feed a subtraction operation: mark the X_i^B input “+” and the X_i^A input “−”. The subtraction output X_i^B-X_i^A branches to C and to absolute value. The absolute-value output |X_i^B-X_i^A| supplies the fourth C input.
C therefore has exactly four incoming feature edges; do not add a second direct post-event edge.
C → Pair proj → Z_i → Flatten → T_i.
Pair proj is a convolutional projection, not an MLP. Z_i also starts the outer dense residual, ending only at the final addition after Reshape.

QUERY UPDATE
Q_i^0 enters MHA_1 at port Q.
Original T_i enters MHA_1 at port K/V.
Place port labels immediately beside their arrowheads on the MHA plate.
MHA_1 output and a direct residual from Q_i^0 enter the first circled plus.
The sum → LN_{q,i} → Q_i^1.
Q_i^1 is an updated query state, not another learnable bank.

SPATIAL-TOKEN UPDATE
Original T_i enters MHA_2 at its left Q port.
Q_i^1 descends into MHA_2 at its upper K/V port.
Do not place K/V text below the block or beside its residual loop.
MHA_2 output and a direct residual from ORIGINAL T_i enter the second circled plus.
The sum → first LN_{t,i} → T_i^1.
T_i^1 branches through FFN and directly into the third circled plus; FFN output supplies the other input.
The sum → second LN_{t,i} → Reshape.
Both LN_{t,i} occurrences reuse the same normalization module. LN_{q,i} is separate. “Shared token LN” is an annotation only, not a data-flow arrow.

DENSE OUTPUT: MANDATORY SERIAL PATH
The second LN_{t,i} has exactly one outgoing data-flow edge, leading to Reshape.
Reshape has one outgoing edge, leading directly to the fourth circled plus.
The other input of that fourth plus is the outer residual from Z_i.
Fourth plus → Conv → CBAM → D_i.
Do not connect either token normalization directly to the fourth plus. Do not bypass Reshape. Do not connect the dense residual after Conv or CBAM. The final plus receives two spatial feature tensors: the reshaped token result and Z_i, not a token sequence. Do not leave a dangling intermediate feature output.

LEGEND AND REFERENCES
Use a compact two-row legend without a surrounding dashed frame:
“C: concatenation”; “⊕: addition”; “MHA: multi-head attention”; “LN: layer normalization”; “FFN: feed-forward network”; “Pair proj: convolutional pair projection”; “A/B: pre/post”.
Include “One scale shown; five independent CQI blocks” once. Query strip segment counts are schematic and do not encode classes or the configured number of queries. No cross-scale query links, low-rank query generation, matching, or prediction masks.
If references are attached, borrow only their tiered layout, typography, feature slabs, and compact operation symbols. No reference architecture overrides this graph. The prompt is complete without local files or previous conversation.

FINAL CONNECTION CHECK
Trace four residuals individually: Q_i^0 around MHA_1, original T_i around MHA_2, T_i^1 around FFN, and Z_i around the entire token route ending after Reshape.
Count exactly four feature inputs to C and four residual addition nodes.
Check the sources of both Q and K/V ports.
Trace the unbranched segment from second LN_{t,i} through Reshape to the final plus.
Check all subscripts and superscripts against the exact lettering list.
Use junction dots only at real branches. Reroute crossings rather than obscuring text. Produce one readable, balanced diagram with no overall title or figure number.

三、参考图使用说明
- 参考图片.png：参考上下层布局、短距离垂直连接和紧凑尺度；不采用其ViT、Bridge和Extractor结构。
- 参考图片2.png：参考特征薄片、短算子、局部残差及轻量浅底强调；不采用多模块拼图、频域处理或匹配流程。
- 参考图片3.png：主要视觉参考，借鉴查询条带、运算符号、端口和清楚的残差；不引入低秩查询生成或其他模块。
参考图不能覆盖第二部分的计算关系。三图可随主提示词上传，模型不需要读取本地路径；未上传时主提示词仍独立完整。旧版单行横向主线要求失效。

四、统一英文局部修订提示词（独立复制）
Correct the attached single-scale CQI figure while retaining its two-tier composition, Times-style font, blue/orange input slabs, green query strips, and subdued backgrounds. Correct all affected connections, not only the input edges. Reposition Reshape, the final addition, and output operators locally when needed to make the corrected path unambiguous.

Typeset these LaTeX labels as mathematics, without printing literal backslashes or underscores: X_i^A, X_i^B, X_i^B-X_i^A, |X_i^B-X_i^A|, Z_i, T_i, Q_i^0, Q_i^1, T_i^1, D_i, \mathrm{LN}_{q,i}, and \mathrm{LN}_{t,i}. Keep plain Q and K/V only as attention-port labels. Use the same i throughout this single-scale diagram.

C must have exactly four inputs: X_i^A, X_i^B, their post-minus-pre difference, and its absolute value. Remove duplicate direct input edges. Mark subtraction inputs + for post and − for pre.
MHA_1 receives Q_i^0 as Q and original T_i as K/V; add Q_i^0 to its output before LN_{q,i}, yielding Q_i^1.
MHA_2 receives original T_i as Q and updated Q_i^1 as K/V; add original T_i before the first LN_{t,i}, yielding T_i^1. Put each port label beside its actual incoming arrowhead.
Add T_i^1 around FFN, then apply the second, shared LN_{t,i}.

Delete any downward or side branch from the segment between the second LN_{t,i} and Reshape. The second LN_{t,i} must feed ONLY Reshape. Connect Reshape directly to the final addition; connect Z_i to its other input. Then connect the sum to Conv, CBAM, and D_i in that order. Remove any dangling slab after Reshape. Keep Reshape adjacent to this final addition so the source is visually unmistakable.

Retain four residual additions, the upper-to-lower Q_i^1 connection, and a clear original-T_i branch. Reduce the top margin and excess background space. Keep the legend in two compact rows without a frame. Do not add mechanisms, expand MHA internals, or preserve incorrect arrows merely to retain the old layout. Check both attention ports and the final spatial addition before rendering.

五、验收清单
- 字符：X的A/B为普通大写上标，i为尺度下标；Q的0/1为状态上标；密集Z_i与token T_i区分；LN_{q,i}与两处LN_{t,i}明确区分。不打印LaTeX转义文本，不混用小型大写字符。
- 输入：C恰好四路，有向差顺序为灾后减灾前，直接灾后输入无重复。
- 注意力：第一次Q来自Q_i^0、K/V来自T_i；第二次Q来自原始T_i、K/V来自Q_i^1。端口标签紧邻实际箭头。
- 残差：Q_i^0、T_i、T_i^1及Z_i四条残差完整；两处token LN共享，query LN独立。
- 密集输出：第二个token LN只接Reshape；Reshape只接最终加法，其另一输入为Z_i；然后Conv、CBAM、D_i。无归一化到最终加法的捷径，无悬空特征块。
- 排版：双层保持，Reshape与最终加法相邻，图例两行无外框；缩至论文宽度检查上下标、箭头和留白。
- 文本核对依据为当前CQI代码及两稿公式；生成后的图片需重新逐箭头核查。两轮针对性修订仍无法纠正的连接停止采用该输出。

这是方法说明图，不是实验图或图文摘要。将来实际使用生成图时，记录网页显示的工具/模型版本、生成日期、原提示词及修订记录，并在图注和文章AI声明披露。当前版本信息不预填，不宣称已经完成生成或作者审核。

六、历史记录（不复制到生成模型）
2026-09-08：正文与图注已区分密集Z_i和展平T_i，说明查询汇聚、上下文返回、残差FFN与密集残差；图4为HCE内部单尺度交互。
2026-09-11：将主提示词和局部修订统一为完整计算契约。撤销旧“仅修输入、其他连接不变”及“单尺度图可省略i”的要求，避免保留Reshape前取值错误或尺度符号不一致。历史工作图不作为正确连接的依据。本轮仅修改本TXT，未生成或验收新图。
