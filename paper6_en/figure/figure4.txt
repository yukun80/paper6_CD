@创建图片 
Create one publication-quality scientific deep-learning diagram showing ONLY the complete internal computation of one Change Query Interaction (CQI) block at a generic scale i.

Do NOT include the full HCE architecture, CNN/DINO encoder, semantic fusion, HA, OSCD, prediction head, flood mask, CFDepth, or any multi-scale routing diagram.
Do NOT add a figure title or figure number.

The figure must present CQI as ONE integrated computation graph, not as several visually separated panels.

The purpose is to clearly explain the original CQI computation:
paired temporal feature construction
→ dense pair representation
→ query update
→ token update
→ token FFN refinement
→ spatial return
→ dense residual return
→ output refinement.

Standard operators such as MHA, DSConv, CBAM, FFN, BN and LN should remain compact boxes.
Do NOT expand their internal textbook structures.
Only the CQI-specific information flow, residual topology, and query/token interaction logic should be explicitly highlighted.

==================================================
CANVAS AND OVERALL STYLE
==================================================

Use a wide landscape canvas, approximately 1.65:1.

Use one large rounded outer container occupying most of the canvas.
Background: white or very pale ivory.
Outline: thin navy.

Use ONE unified background for the whole module.
Do NOT split the background into three different colored zones.
If mild grouping is needed, use only very subtle local pale tints behind small regions, but the figure must remain visually unified as a single module.

Use a draw.io-like scientific style consistent with the model part of Figure 2:

- Times New Roman-like text
- italic mathematical symbols
- thin navy or black orthogonal arrows
- pastel feature slabs
- pale blue pre-event feature
- pale orange post-event feature
- neutral gray dense feature / token
- pale green learnable query
- pale orange MHA boxes
- pale blue LN / FFN boxes
- pale green DSConv box
- pale lavender CBAM and output feature
- no gradients
- no glossy effects
- no heavy shadows
- no realistic imagery
- no paragraph text boxes

Reserve about 86–88% of the figure height for the main computation.
Use only a compact bottom strip for the legend.

==================================================
LAYOUT PRINCIPLE
==================================================

Use a horizontally compact single-module layout with one dominant main path.

The token branch should form the horizontal visual backbone of the figure.

Recommended overall spatial organization:

left ~26%:
paired temporal feature construction + pair projection

middle ~42%:
token sequence backbone with query-update branch above it

right ~32%:
FFN, reshape, dense residual return, output refinement

Do NOT leave a large empty upper area.
Do NOT place the query update and token update as two widely separated islands.
The upper query path should be a compact branch attached closely above the central token backbone.

==================================================
EXACT NOTATION
==================================================

Use correct mathematical notation:

X_i^A
X_i^B
X_i^B − X_i^A
|X_i^B − X_i^A|
Z_i
T_i
Q_i^0
Q_i^1
T_i^1
D_i
LN_{q,i}
LN_{t,i}

A and B are uppercase temporal superscripts.
i is the generic scale subscript.
0 and 1 are state superscripts.

Do not print literal LaTeX escape characters.

==================================================
1. PAIRED TEMPORAL FEATURE CONSTRUCTION
==================================================

At the far left place two feature slabs:

upper:
X_i^A
blue

lower:
X_i^B
orange

Construct the directed difference:
X_i^B − X_i^A

Use one compact subtraction operator.
Clearly preserve the direction: post-event minus pre-event.

The directed difference branches in two ways:
1. directly to concatenation
2. through an Abs operator to produce |X_i^B − X_i^A|

Both original X_i^A and X_i^B must also go directly to the same concatenation.

Use one circled C with exactly four inputs:

X_i^A
X_i^B
X_i^B − X_i^A
|X_i^B − X_i^A|

Place these four-input relations compactly so that the left side does not become too tall.

==================================================
2. DENSE PAIR REPRESENTATION
==================================================

After concatenation draw the implemented pair projection compactly:

1×1 Conv
→ BN
→ SiLU
→ DSConv
→ Z_i

Do not expand DSConv.
Keep DSConv as one compact box.

Draw Z_i as one gray spatial feature slab.

Z_i branches into TWO routes:

1. main route:
Z_i → Flatten → T_i

2. dense residual route:
Z_i → a clean long residual path that returns only after Reshape

Place Flatten and T_i near the horizontal centerline of the figure so that T_i can serve as the visual backbone.

Draw T_i as a short horizontal gray token strip.

==================================================
3. QUERY UPDATE
==================================================

Place Q_i^0 above the T_i region, close to the center of the figure, not too high.

Draw Q_i^0 as a green token strip.

Use one compact attention box:
MHA_1

Explicitly label input roles near the incoming arrows:

Q_i^0 → Q
T_i → K/V

MHA_1 output enters one circled addition.
The second input of this addition is a direct residual from Q_i^0.

Exactly two inputs enter this addition.

Then:
addition → LN_{q,i} → Q_i^1

Draw Q_i^1 as a green updated query strip.

Use only one small local label near this branch:
Query update

Do not use a large spanning caption such as “Tokens → queries” across a wide blank area.

==================================================
4. TOKEN UPDATE
==================================================

Keep the token backbone visually continuous.

Place MHA_2 directly to the right of the T_i region, roughly along the horizontal midline.

Inputs:

original T_i → Q
updated Q_i^1 → K/V

Again place the role labels Q and K/V right beside the actual incoming arrowheads.

MHA_2 output enters one circled addition.
Its residual input is the ORIGINAL T_i.

Exactly two inputs enter this addition.

Then:
addition → LN_{t,i} → T_i^1

Draw T_i^1 as a gray updated token strip.

Use only one small local label:
Token update

Do not place oversized title text above wide blank space.

==================================================
5. TOKEN FFN REFINEMENT
==================================================

From T_i^1 create the FFN residual block.

Main path:
T_i^1 → FFN → circled addition

Residual:
T_i^1 → same addition

FFN remains one compact box.
Do not expand Linear–GELU–Linear.

Then:
addition → LN_{t,i} → Reshape

The two LN_{t,i} blocks share parameters.

Show this with a compact dashed bracket or short dashed annotation placed close to the two LN boxes:
shared LN_{t,i}

The dashed sharing annotation must be short and compact.
Do not let it occupy large empty upper-right space.

==================================================
6. SPATIAL RETURN AND DENSE RESIDUAL
==================================================

After Reshape, draw one gray spatial feature slab.

This reshaped dense feature enters one circled addition.

The other input is the long residual from original Z_i.

Thus:
Reshape output + Z_i → dense spatial feature

Keep this addition close to Reshape and to the later output projection so that the dense residual purpose is visually clear.

The residual path from Z_i should run neatly along the lower edge of the main graph, but not too low.
Do not make the residual loop excessively large.

==================================================
7. OUTPUT REFINEMENT
==================================================

After the dense residual addition, use:

DSConv
→ CBAM
→ D_i

Do not expand DSConv or CBAM.
Keep both as compact standard boxes.

Draw D_i as one purple feature slab.

D_i is a change feature, not a prediction mask.

==================================================
VISUAL COMPOSITION IMPROVEMENTS
==================================================

The main flow must occupy the canvas compactly, with minimal wasted space above the graph.

Use the token path as the central horizontal axis:
Z_i → Flatten → T_i → MHA_2 region → LN_t → T_i^1 → FFN → LN_t → Reshape → + Z_i → DSConv → CBAM → D_i

Place the query-update branch immediately above the T_i / MHA_2 region, close enough that the figure reads as one coupled mechanism.

Keep the vertical distance between Q_i^0 / MHA_1 / Q_i^1 and the token backbone modest.
Avoid large empty whitespace in the upper half.

The final figure should look like one compact and coherent CQI block, not like three separate stages pasted together.

==================================================
LEGEND
==================================================

Use a compact single-row or at most two-row legend in a narrow bottom strip.
Do not surround the legend with a large heavy panel.

Include only:

blue slab = pre-event feature
orange slab = post-event feature
gray slab/token = pair feature / token
green strip = learnable query
purple slab = change feature
C = concatenation
⊕ = addition
MHA = multi-head attention
LN = layer normalization

Do not include long descriptive phrases.

==================================================
FORBIDDEN ELEMENTS
==================================================

Do not include:
- full HCE overview
- HA
- CNN
- DINO
- semantic fusion
- OSCD
- pyramid levels or scale-routing
- DSConv internals
- CBAM internals
- MHA internals
- lower detail panels
- prediction head
- flood map
- decorative background segmentation into three colored blocks

Do not visually overemphasize generic operators more than CQI-specific logic.

==================================================
FINAL AUDIT
==================================================

Before rendering verify:

1. The whole CQI is drawn as ONE integrated module.
2. There is no large empty upper region.
3. The background is unified, not split into three obvious color zones.
4. The token path forms the main horizontal visual backbone.
5. Query update is a compact upper branch attached closely to the token backbone.
6. C receives exactly four inputs:
   X_i^A
   X_i^B
   X_i^B−X_i^A
   |X_i^B−X_i^A|
7. Z_i branches only into:
   Flatten → T_i
   and the long dense residual.
8. MHA_1 uses Q_i^0 as Q and T_i as K/V.
9. MHA_2 uses original T_i as Q and Q_i^1 as K/V.
10. FFN has a residual from T_i^1.
11. Reshape occurs before the final dense residual addition.
12. The final dense residual comes from original Z_i.
13. The shared LN_{t,i} annotation is short and compact.
14. DSConv and CBAM remain simple boxes.
15. The legend is compact and does not consume excessive height.

Deliver one compact, visually unified, publication-quality CQI scientific diagram with no overall title or figure number.