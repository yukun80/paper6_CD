@创建图片 
Create one publication-quality scientific deep-learning diagram showing ONLY the internal computation of one Harmonized Alignment (HA) block at a generic scale i.

Do NOT include the full HarmoSSM architecture, CNN/DINO encoder, semantic fusion, CQI, OSCD, prediction head, flood map, or any multi-scale routing diagram.

Do NOT add a figure title or figure number.

The purpose of this figure is to make the complete internal HA computation readable from left to right:
paired temporal features
→ pair-shared statistical calibration
→ residual feature recovery
→ deformable local alignment
→ harmonized paired outputs.

Use a generic scale index i only. Do not discuss which pyramid levels use HA; that information is already shown in the overall framework figure.

==================================================
CANVAS AND SCIENTIFIC STYLE
==================================================

Use a wide landscape canvas, approximately 1.55:1.

Use one large rounded container occupying most of the canvas.
Background: white or very pale ivory.
Outline: thin navy.
Do not divide the computation into multiple large independent panels.

Use a draw.io-like scientific network-diagram style consistent with the model portion of Figure 2:

- Times New Roman-like typography
- italic mathematical variables
- thin navy or near-black orthogonal arrows
- restrained pastel operator boxes
- shallow axonometric feature slabs
- pre-event features in pale blue
- post-event features in pale orange
- calibration operators in pale neutral blue
- alignment/attention operators in pale cyan or lavender
- no gradients
- no glossy effects
- no heavy shadows
- no realistic SAR imagery
- no paragraph-style text inside boxes

Arrange the MAIN computation across the upper ~68–72% of the canvas.

Use the remaining lower space for TWO compact explanatory details:
1. shared recovery block detail
2. deformable sampling / offset-prediction detail

These are enlarged explanations of reused operations in the main computation, not additional sequential processing.

Reserve only a very narrow bottom strip for a compact legend.

==================================================
EXACT NOTATION
==================================================

Use proper mathematical typography.

Inputs:
F_i^A
F_i^B

Calibration intermediates:
Y_i^A
Y_i^B
U_i^A
U_i^B

Outputs:
X_i^A
X_i^B

Pair-shared statistics:
μ_i^{AB}
σ_i^{AB}

Canonical learned parameters:
μ_i^c
σ_i^c

Alignment quantities:
Q
K
V
Δp
b
p′ = p + r + Δp
K_s
V_s
α

A = pre-event
B = post-event

Do not print raw LaTeX commands.

==================================================
MAIN COMPUTATION — LEFT: PAIR-SHARED CALIBRATION
==================================================

Place two input feature slabs at the far left:

upper:
F_i^A
blue

lower:
F_i^B
orange

Each feature must branch in exactly TWO directions:
1. to its own normalization data input;
2. to one shared Joint moments operator.

JOINT MOMENTS
Place one compact operator centrally between the two temporal paths:

Joint moments

Both F_i^A and F_i^B enter it.

Joint moments outputs:

μ_i^{AB}
σ_i^{AB}

Route the SAME pair-shared statistics to BOTH normalization operators.

Do not show independent temporal statistics.

PRE-EVENT CALIBRATION PATH
F_i^A
+ μ_i^{AB}, σ_i^{AB}
→ Norm
→ Canonical affine
→ Y_i^A
→ Recovery
→ U_i^A

POST-EVENT CALIBRATION PATH
F_i^B
+ μ_i^{AB}, σ_i^{AB}
→ Norm
→ Canonical affine
→ Y_i^B
→ Recovery
→ U_i^B

Place one concise annotation between the two Canonical affine operators:

shared learned μ_i^c, σ_i^c

Do not connect the two affine blocks with a data-flow arrow.

==================================================
CALIBRATION VISUAL CUE
==================================================

Near Joint moments and Norm, add one small deep-learning-style feature-statistics schematic.

Show:
- one short blue multi-channel feature strip
- one short orange multi-channel feature strip
- both entering a shared μ / σ symbol
- two normalized output strips centered with respect to the same reference statistics

This is only a compact visual cue for pair-shared normalization.

Do NOT draw histograms.
Do NOT draw a separate calibration submodule box.
Do NOT use long explanatory labels.

==================================================
MAIN COMPUTATION — RECOVERY
==================================================

In the main graph, show a compact identical operator labeled:

Recovery

after Y_i^A and Y_i^B.

Recovery weights are shared across the two temporal paths.

The detailed internal structure of Recovery must be expanded ONCE in the lower-left blank area:

Y_i^t
→ 3×3 Conv
→ BN
→ SiLU
→ 1×1 Conv
→ BN
→ circled addition
→ U_i^t

The second input of the addition is a direct residual from Y_i^t.

Use:
t ∈ {A,B}

Add only one short note:

shared across A/B

The residual MUST start from Y_i^t after Canonical affine.

Do not draw raw F_i^t as a residual into Recovery.

==================================================
MAIN COMPUTATION — RIGHT: DEFORMABLE LOCAL ALIGNMENT
==================================================

U_i^A and U_i^B must continue directly from calibration into alignment.
Do not redraw them as disconnected duplicate inputs.

The post-event feature provides query guidance.
The pre-event feature provides key and value features.

Q PROJECTION
U_i^B
→ 1×1 Conv
→ Q

K PROJECTION
U_i^A
→ 1×1 Conv
→ K

V PROJECTION
U_i^A
→ 1×1 Conv
→ V

Use three small projection plates aligned vertically or diagonally.

==================================================
OFFSET AND ATTENTION-BIAS PREDICTION
==================================================

Compute:

U_i^B − U_i^A

Then concatenate exactly THREE tensors:

U_i^A
U_i^B
U_i^B − U_i^A

using one circled C.

The concatenated feature enters:

Grouped 3×3 Conv
→ BN
→ SiLU
→ 1×1 Conv

The final output splits into TWO independent branches:

Branch 1:
offset logits
→ tanh × scale
→ Δp

Branch 2:
→ b

Do NOT apply tanh to b.

==================================================
REFERENCE POSITIONS AND DEFORMABLE SAMPLING
==================================================

Show a compact coordinate operator:

p′ = p + r + Δp

where:
p = current feature-grid location
r = fixed local reference offset
Δp = learned displacement

The same p′ must feed BOTH key and value sampling.

K + p′
→ Bilinear sampling
→ K_s

V + p′
→ Bilinear sampling
→ V_s

Keep these as two separate sampling operators.

==================================================
DEFORMABLE SAMPLING VISUAL DETAIL
==================================================

Use the lower-right blank area to expand the sampling idea visually.

Draw one small regular feature grid:
- dark blue dots = reference sample locations
- orange/red dots = displaced locations
- short arrows = learned offsets Δp

Add only these labels:

Reference
Displaced
Δp

This visual must clearly communicate local deformable sampling.

Do not use a realistic image.
Do not use a heatmap.
Do not imply that the number of displayed points equals the configured number of sampling points.

==================================================
LOCAL ATTENTION AND AGGREGATION
==================================================

Q and K_s enter one score operator:

Q K_s^T / √d

Its output and b enter one circled addition.

Exactly TWO inputs enter this addition.

Then:

addition
→ Softmax
→ α

Use α as the attention weights.

Then:

α
+
V_s
→ Weighted sum

Represent this as a small weighted-aggregation operator with two input ports:
weights α
values V_s

Then:

Weighted sum
→ 1×1 Conv
→ BN
→ circled addition
→ X_i^A

The second input of this final addition is the ORIGINAL U_i^A.

This is the pre-event alignment residual.

==================================================
POST-EVENT OUTPUT
==================================================

U_i^B must continue on a clean direct path to:

X_i^B

The post-event feature is NOT updated by the deformable attention.

Therefore the final paired outputs are:

X_i^A : locally aligned pre-event feature
X_i^B : post-event reference feature

Do not add any extra fusion after these outputs.

==================================================
FLOW ORGANIZATION
==================================================

The overall visual reading order must be obvious:

F_i^A / F_i^B
→ shared calibration
→ U_i^A / U_i^B
→ deformable local alignment
→ X_i^A / X_i^B

Calibration should occupy roughly the left 38–42% of the main graph.
Alignment should occupy roughly the right 58–62%.

Keep A and B temporal paths visually parallel where possible.

Use junction dots only for true branches.
Use circled C only for concatenation.
Use circled plus only for element-wise addition.

==================================================
LEGEND
==================================================

Keep the legend minimal:

blue slab: pre-event feature
orange slab: post-event feature
C: concatenation
⊕: element-wise addition
Q/K/V: query/key/value
BN: batch normalization

No explanation paragraphs.

==================================================
FORBIDDEN ELEMENTS
==================================================

Do not draw:
- multi-scale pyramid routing
- P1/P2/P3/P4/P5
- CNN
- DINO
- semantic fusion
- CQI
- OSCD
- prediction masks
- flood extent
- scale-selection logic
- separate large “Calibration module” and “Alignment module” panels

Do not hide:
- Joint moments
- shared normalization statistics
- canonical affine
- Recovery residual
- Q/K/V source directions
- offset predictor
- Δp
- b
- bilinear sampling
- softmax weighting
- weighted V aggregation
- final pre-event residual

==================================================
FINAL CONNECTION AUDIT
==================================================

Before rendering verify:

1. F_i^A and F_i^B BOTH enter Joint moments.
2. Each F_i^t also enters its own Norm.
3. The same μ_i^{AB}, σ_i^{AB} enter BOTH Norms.
4. Canonical affine outputs are Y_i^A and Y_i^B.
5. Recovery residual begins at Y_i^t.
6. Recovery structure is shown once in detail:
   3×3 Conv → BN → SiLU → 1×1 Conv → BN + residual.
7. U_i^B produces Q.
8. U_i^A produces K and V.
9. Offset predictor receives U_i^A, U_i^B and U_i^B−U_i^A.
10. Δp enters sampling coordinates only.
11. b enters attention-score addition only.
12. K and V are sampled using the SAME displaced locations p′.
13. Q interacts with sampled K_s.
14. softmax weights aggregate V_s.
15. aligned result is projected and added to original U_i^A.
16. U_i^B directly becomes X_i^B.
17. no unrelated network component appears.

Deliver one complete, fully expanded HA module diagram with no figure title.