base_config: base.yaml

train:
  run_name: stage4_full_prompt_refine
  epochs: 40

model:
  enable_router: true
  enable_memory: true
  enable_prompt_refiner: true

loss:
  w_router: 0.3
  w_open: 0.6
  w_urban: 1.0
  w_consistency: 0.1
  w_boundary: 0.3
