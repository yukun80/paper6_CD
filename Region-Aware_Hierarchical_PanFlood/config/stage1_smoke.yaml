base_config: stage1_minimal.yaml

train:
  run_name: stage1_smoke
  epochs: 1
  batch_size: 2
  val_batch_size: 1
  num_workers: 0
  log_interval: 1

data:
  train_split: Region-Aware_Hierarchical_PanFlood/config/smoke_train.txt
  val_split: Region-Aware_Hierarchical_PanFlood/config/smoke_val.txt
