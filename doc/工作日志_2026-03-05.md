# 工作日志（2026-03-05）

## 归档说明（已失效）
- 本页原记录为 `panopticon/urban_floods` 三分类分割适配过程及其配套评估策略。
- 该路线已不再作为当前项目默认实现，相关命令与指标不应继续用于现行实验。

## 已弃用内容提示
- 包括但不限于：
  - Panopticon 下游训练/评估入口；
  - 历史 `selection_metric` 方案（如 `pos_mIoU` 优先）；
  - 当时的数据通道定义与对应配置口径。

## 当前有效路线
- 当前项目主线为 `ChangeDINO-main` 变化检测流程。
- 执行与协作规范请以 `AGENTS.md` 和 `doc/工作日志_2026-03-12.md` 为准。
