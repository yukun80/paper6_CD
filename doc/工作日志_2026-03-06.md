# 工作日志（2026-03-06）

## 归档说明（已失效）
- 本页原记录为 UrbanSARFloods 的历史扩展路线，包含：
  - `8 -> 12` 通道扩展与通道顺序规范；
  - 层次化标签（`GT_floodness/GT_flood_type`）；
  - `urban_floods_hier` 训练与评估口径；
  - 历史 `pos_mIoU` 主指标与 tie-breaker 选择策略。
- 以上内容均已从当前默认工作流中退役，不再作为有效实现规范。

## 清理后的执行原则
- 不再引用本页中的 12 通道顺序、层次化监督和 `pos_mIoU` 优先策略。
- 当前项目协作、命令与指标口径以 `ChangeDINO-main` 主线为准。

## 当前有效路线
- 现行规范：`AGENTS.md`
- ChangeDINO 适配记录：`doc/工作日志_2026-03-12.md`

## 备注
- 若未来确需重启本页方向，应新建独立路线文档并重新定义数据口径、指标与训练流程。
