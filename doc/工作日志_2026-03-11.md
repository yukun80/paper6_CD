# 工作日志（2026-03-11）

## 1. 本次目标
- 基于 `PPO-main` 的 prompt optimization 思路，构建面向 `datasets/GF3_Henan` 的双时相 SAR 洪水/变化分割流程。
- 将 `sar_prompt_flood` 收敛为 `GF3_Henan-only` 工具，不再适配 `UrbanSARFloods`。
- 将分割后端重构为纯 `SAM vit_b`，删除启发式分割分支，并补齐权重下载脚本。

## 2. 数据与任务重新定义
- 输入数据：
  - `datasets/GF3_Henan/Pre_Zhengzhou_ascending_clip.tif`
  - `datasets/GF3_Henan/Post_Zhengzhou_ascending_clip.tif`
- 数据特征：
  - 单波段 GF3 HV 极化 SAR 幅度图；
  - 两景尺寸、CRS、分辨率一致，但 `transform` 不完全一致；
  - 存在 `nodata=-3.4028235e38`，不能直接逐像素做变化检测。
- 当前任务定义：
  - 输入：灾前/灾后 GF3 双时相 SAR
  - 输出：全图二值变化/洪水 mask
  - 监督条件：当前无标签，采用无标注 prompt 优化 + SAM 分割。

## 3. `sar_prompt_flood` 重构内容

### 3.1 GF3_Henan-only 数据与配置
- 保留并收敛模块：
  - `sar_prompt_flood/config.py`
  - `sar_prompt_flood/config/gf3_henan.json`
  - `sar_prompt_flood/gf3_preprocess.py`
  - `sar_prompt_flood/data.py`
  - `sar_prompt_flood/prompts.py`
  - `sar_prompt_flood/optimizer.py`
  - `sar_prompt_flood/run_gf3_pipeline.py`
- 删除 `UrbanSARFloods/CH12` 适配思路，配置和主流程不再保留多数据集兼容分支。
- 默认处理目录：
  - 输入：`datasets/GF3_Henan`
  - 预处理输出：`datasets/GF3_Henan_processed`
  - 推理输出：`sar_prompt_flood/outputs/gf3_henan`

### 3.2 GF3 预处理链路
- 新增/落地入口：
  - `datasets/script/prepare_gf3_henan_pair.py`
  - `sar_prompt_flood/gf3_preprocess.py`
- 预处理步骤：
  - 以 `Pre` 为参考重采样 `Post`
  - 统一清洗 `nodata`
  - 构造 `diff / log_ratio_like / change_score`
  - 切成 `512x512`、`overlap=128` 的 tiles
  - 生成 `manifest.json` 与对齐后的 GeoTIFF/NPZ 数据

### 3.3 Prompt 生成与优化
- `sar_prompt_flood/prompts.py`
  - 正点从高 `change_score` 连通区域生成；
  - 负点从稳定低变化区和边界外环生成；
  - 节点特征改为单极化双时相统计，不再依赖 DINO 或 CH12 通道。
- `sar_prompt_flood/optimizer.py`
  - 保留 prompt 子集优化骨架；
  - 优化阶段直接调用 SAM 输出 mask，并据此计算无标注目标函数；
  - 目标函数由 `overlap_high_change / mask_focus / coverage / cross_sep / redundancy / fragments / area_ratio` 组成。

## 4. 纯 SAM 分割重构

### 4.1 删除启发式分割分支
- 重构文件：
  - `sar_prompt_flood/segmenter.py`
  - `sar_prompt_flood/run_gf3_pipeline.py`
  - `sar_prompt_flood/__init__.py`
  - `sar_prompt_flood/README.md`
- 已删除：
  - `HeuristicFloodSegmenter`
  - `build_segmenters`
  - `select_final_mask`
  - 所有 fallback/backend 选择逻辑
- 当前约束：
  - 只支持 `segmenter.backend = "sam"`
  - 若权重缺失或 `segment_anything` 初始化失败，直接报错

### 4.2 SAM 权重准备脚本
- 新增脚本：
  - `datasets/script/download_sam_vit_b.py`
- 固定下载地址：
  - `https://dl.fbaipublicfiles.com/segment_anything/sam_vit_b_01ec64.pth`
- 固定落盘路径：
  - `PPO-main/segmenter/checkpoint/sam_vit_b_01ec64.pth`
- 行为：
  - 自动创建目录
  - 若已有有效文件则直接跳过
  - 下载到 `.part` 临时文件后原子替换

### 4.3 运行方式
```bash
# 下载权重
python datasets/script/download_sam_vit_b.py

# GF3 预处理
python datasets/script/prepare_gf3_henan_pair.py \
  --config-file sar_prompt_flood/config/gf3_henan.json

# 全流程推理
python -m sar_prompt_flood.run_gf3_pipeline \
  --config-file sar_prompt_flood/config/gf3_henan.json
```

## 5. 当前验证状态
- 配置核对通过：
  - `sar_prompt_flood/config/gf3_henan.json` 已与 `config.py` 对齐
  - `sam_checkpoint` 指向 `PPO-main/segmenter/checkpoint/sam_vit_b_01ec64.pth`
- 静态检查通过：
  - `python -m py_compile sar_prompt_flood/*.py datasets/script/download_sam_vit_b.py datasets/script/prepare_gf3_henan_pair.py`
- 本地文件核对：
  - 权重文件已存在：`PPO-main/segmenter/checkpoint/sam_vit_b_01ec64.pth`
  - 文件大小约 `358MB`

## 6. 当前结论
- `sar_prompt_flood` 现已从“GF3 + SAM/heuristic 双后端原型”收敛为“GF3_Henan-only + 纯 SAM 分割”版本。
- 当前研究链路可以明确表达为：
  - `GF3 双时相预处理 -> 变化先验 -> prompt 生成 -> prompt 优化 -> SAM 分割 -> 全图回拼`
- 下一阶段重点应转向：
  - 小规模 smoke 与全图推理验证；
  - 检查 `init_mask -> opt_mask -> final_mask` 的面积变化与空间合理性；
  - 若纯 SAM 优化过慢，再单独评估 surrogate objective，而不是重新加回 heuristic 分割器。
